# Regra do scratch/

Toda primeira versão de uma solução nasce aqui, em `.txt`, nunca `.java` — `.txt` não aciona autocomplete nem análise da IDE, forçando a prática de escrever código do zero, sem assistência de ferramenta.

Só depois de declarar "terminei" o código migra para `.java` em `../solucoes/`.
